<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="student.projects.jetpackpam">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Optional: Android 13+ runtime permission -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".util.JetPackPamApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/pamicon"
        android:label="P.A.M"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetPackPam"
        android:usesCleartextTraffic="true">

        <!-- Main launcher activity -->
        <activity
            android:name="student.projects.jetpackpam.MainActivity"
            android:exported="true"
            android:label="P.A.M"
            android:theme="@style/Theme.JetPackPam">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Biometric transient authentication Activity -->
        <activity
            android:name="student.projects.jetpackpam.screens.accounthandler.authorization.BiometricPromptActivity"
            android:exported="false"
            android:label="BiometricPromptActivity"
            android:theme="@style/Theme.JetPackPam" />

        <!-- Firebase Messaging Service -->
        <service
            android:name="student.projects.jetpackpam.util.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
